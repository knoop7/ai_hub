language: "zh-CN"
intents:
  HassVacuumSetMode:
    use_home_assistant: true
    data:
      - sentences:
          - "[<let>] <set> {name}[清扫模式]"
          - "[<let>] {name}[切换到][清扫模式]"
          - "[<let>] <set> {name}[为][清扫模式]"
        requires_context:
          domain: "vacuum"