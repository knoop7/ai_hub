language: "zh-CN"
intents:
  HassSetPosition:
    use_home_assistant: true
    data:
      - sentences:
          - "[<let>] <set><name>{position}"
          - "[<let>] [把]<name>{position}<set_to>"
          - "[<let>] <set>[<area>]<valve_names>{position}"
        requires_context:
          domain: valve