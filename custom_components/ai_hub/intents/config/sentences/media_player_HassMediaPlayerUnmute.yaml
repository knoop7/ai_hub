language: "zh-CN"
intents:
  HassMediaUnmute:
    use_home_assistant: true
    data:
      - sentences:
          - "[<let>] <turn_on>{name}"
          - "[<let>] <turn_on>[声音]{name}"
          - "[<let>] {name}<turn_on>"
        requires_context:
          domain: "media_player"
